version: 2.0
jobs:
 build:
   docker:
     - image: microsoft/dotnet
   working_directory: ~/neteoc
   steps:
     - checkout
     - run: cd Source && dotnet restore && dotnet publish
     - run: ls -R Source/bin/ | awk '
     - run: ls Source/bin/Release/PublishOutput 
     - run: ssh neteoc:${netEocPass}@${boxIP} && echo $HOSTNAME
