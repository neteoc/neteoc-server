version: 2.0
jobs:
 build:
   docker:
     - image: microsoft/dotnet
   working_directory: ~/neteoc
   steps:
     - checkout
     - run: cd Source && dotnet restore && dotnet publish
     - run: scp -r Source/Services/NetEOC.Auth/bin/Debug/netcoreapp1.0/publish neteoc:'${netEocPass}'@${boxIP}:/home/ubuntu/delme/
